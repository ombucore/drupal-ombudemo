<?php

/**
 * @file 
 * Contains tests for the Bean styles module.
 */

/**
 * Test case.
 */
class BeanStyleTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Bean styles tests'),
      'description' => t('TODO: write me.'),
      'group' => t('Bean styles'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('bean_style'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}